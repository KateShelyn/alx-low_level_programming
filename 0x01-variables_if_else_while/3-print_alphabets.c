#include <studio.h>
/**
 * main - print if number
 *
 * Description - using main function
 * this program prints
 * Reaturn: 0
 */
int main (void)
{
	char c;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	for (ch = 'A'; ch <= 'Z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
