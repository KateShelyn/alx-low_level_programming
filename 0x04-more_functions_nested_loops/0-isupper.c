#include "main.h"
/**
 * main - a function that prints uppercase characters
 * Description - using main function
 * Return: 0
 */
int _isupper(int c)
{
	if (c => 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
